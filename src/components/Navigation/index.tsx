import styles from './index.module.css';

export const Navigation: React.FC = () => {
  return <nav className={styles.container}>{/* <a className={styles.link} href={PageUrl.About}>About</a> */}</nav>;
};
